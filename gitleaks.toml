title = "Shironeko Allocate – dev allowlist"

[allowlist]
description = "allow known local/dev credentials and compose examples"
paths = [
  "docker-compose.yml"
]
regexes = [
  "(?i)MYSQL_ROOT_PASSWORD=root",
  "(?i)MYSQL_PASSWORD=app",
  "(?i)DB_PASSWORD=app",
  "(?i)DB_USERNAME=app",
  "(?i)MYSQL_USER=app"
]

